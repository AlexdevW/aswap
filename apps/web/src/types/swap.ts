// import { Chain } from "viem"

export type Token = {
  name: string
  symbol: string
  icon: string
  decimals: number
  address: `0x${string}`
  // availableChains: {
  //   chain: Chain
  //   contract?: string
  // }[]
}
